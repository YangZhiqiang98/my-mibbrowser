{"file": ".trellis/spec/frontend/mib-tree-snmp-ops.md", "reason": "Defines the resolveBulkOids contract we're modifying (lines 13-48). Also documents Result-Column resolution rules used by buildResultSession (segment-boundary prefix + longest-prefix wins) which our buildTableSession's column matching mirrors. CRITICAL: the spec's current 'kind === column' text in resolveBulkOids semantics (line 41-42) is itself part of the bug — implementer must update this spec alongside the code so spec and code stay in sync."}
{"file": ".trellis/spec/frontend/quality-guidelines.md", "reason": "Frontend code standards (TS strict, no any, immutable, ~200-line component cap, error handling on IPC). Also defines test scope: Vitest + jsdom, unit tests for pure renderer utilities, OID helpers must cover segment-boundary prefix — our new tests must follow this conventions section."}
{"file": ".trellis/spec/frontend/type-safety.md", "reason": "Type organization conventions: where to put new types (component-local vs shared), import patterns for cross-process types. Our isTableColumnChild helper takes MibTreeNodeData; this file documents where the type lives and how to import it."}
{"file": ".trellis/spec/guides/code-reuse-thinking-guide.md", "reason": "We're extracting isTableColumnChild precisely to avoid the 4-site duplication. The guide's 'Search First / Use or extend' principle is the lens for the extraction: confirm no existing helper does this, then place the new helper where both tableSession.ts and MibTreePanel.tsx can reuse it (tableSession.ts is the right home — it already owns resolveTableTarget)."}
{"file": ".trellis/spec/frontend/component-guidelines.md", "reason": "We're not adding a new component, but the GETBULK fan-out edit lives inside MibTreePanel.tsx — implementer should follow this file's structure conventions (imports order, explicit return types) when touching it, even for a tiny helper-call swap."}

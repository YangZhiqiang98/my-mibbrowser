{"file": ".trellis/spec/frontend/mib-tree-snmp-ops.md", "reason": "Verifies the resolveBulkOids contract is honored: kind === 'table' still finds the entry child first; kind === 'entry' still falls back to [node.oid] on empty; the fallback never returns []. Also verifies the spec text itself was updated to match the new column-resolution rule (was buggy — said 'kind === column' only). Cross-check: Result-Column resolution (longest-prefix, segment-boundary) — our buildTableSession's isOidWithinPrefix uses the same predicate; reviewer must confirm the rule wasn't accidentally weakened during refactor."}
{"file": ".trellis/spec/frontend/quality-guidelines.md", "reason": "Reviewer checks: TypeScript strict types on isTableColumnChild signature; tests added under tests-required scope (pure renderer utility → must have unit tests); no console.log; no any; immutable patterns preserved. Also: confirm npm test / npm run typecheck / npm run lint all pass after the change."}
{"file": ".trellis/spec/guides/code-reuse-thinking-guide.md", "reason": "Reviewer verifies the extracted helper actually consolidates the four duplicated filter sites — no remaining 'kind === column' filter for entry/table children leaks in either tableSession.ts or MibTreePanel.tsx. If reviewer finds a fifth caller of the same pattern that wasn't switched, that's a regression."}
{"file": ".trellis/spec/frontend/type-safety.md", "reason": "Reviewer confirms: the new helper's parameter / return types are explicit; MibTreeNodeData import follows the documented cross-module pattern (from renderer types/index.ts); no new any sneaks in."}
